language: node_js
node_js: stable

branches:
  only:
    - blog-source
env:
 global:
   - GH_REF: github.com/mzeht/mzeht.github.io.git

# S: Build Lifecycle
install:
  - npm install


#before_script:
 # - npm install -g gulp

before_install:
- export TZ='Asia/Shanghai'
- npm install -g hexo
- npm install -g hexo-cli

# 执行的命令
script:
- hexo clean
- hexo generate
# 执行的成功后执行
#after_success:
#- hexo deploy

after_script:
  - cd ./public
  - git init
  - git config user.name "mzeht"
  - git config user.email "mzeht8303@gmail.com"
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master
# E: Build LifeCycle

